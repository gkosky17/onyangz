function takeSteps(totalSteps) {
    for (let i = 1; i <= totalSteps; i++) {
      console.log(`Step ${i}`);
    }
  }
  
  // Example usage:
  takeSteps(5); // This will print "Step 1", "Step 2", ..., "Step 5"
  